export default function(messageId, userId, userName, messageText, time) {
    this.messageId = messageId
    this.userId = userId
    this.userName = userName
    this.messageText = messageText
    this.time = time
}